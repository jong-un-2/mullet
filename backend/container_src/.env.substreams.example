# Substreams PostgreSQL Sink Configuration
# Copy this file to .env.substreams and fill in your values

# PostgreSQL Connection (Neon Direct - NOT pooler)
# Substreams requires direct connection for long-lived streaming
SUBSTREAMS_SINK_POSTGRES_DSN=postgresql://neondb_owner:npg_9kucNXKpA6eC@ep-solitary-heart-a19z1rs7.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Substreams API Configuration
SUBSTREAMS_ENDPOINT=mainnet.sol.streamingfast.io:443
SUBSTREAMS_API_KEY=your_api_key_here

# Get API key from: https://app.streamingfast.io/

# Network Configuration
SUBSTREAMS_NETWORK=solana-mainnet

# Start Block (where to begin indexing)
# Mars V13 deployment (Oct 15, 2025) with claim_farm_rewards
# Program ID: DojYM71BG5FoCEMgd1sHtodAjQQtGX271swjaDrtHaY4
SUBSTREAMS_START_BLOCK=372182088

# Performance Tuning
SUBSTREAMS_FLUSH_INTERVAL=1s       # How often to write to DB (1s = low latency, 10s = higher throughput)
SUBSTREAMS_FINAL_BLOCKS_ONLY=false # true = only finalized blocks (slower), false = all blocks (faster)
SUBSTREAMS_BATCH_SIZE=100          # Number of blocks to process in one batch

# Logging
SUBSTREAMS_LOG_LEVEL=info          # debug, info, warn, error
